@using Epam.WebPages.UIweb.Models;
@{
    WebImage awardimage = null;

    if (IsPost)
    {
        var title = Request["title"];
        awardimage = WebImage.GetImageFromRequest();
        AwardModel.AddAward(title, awardimage);
        Response.Redirect("~/Pages/index.cshtml");
    }
}
