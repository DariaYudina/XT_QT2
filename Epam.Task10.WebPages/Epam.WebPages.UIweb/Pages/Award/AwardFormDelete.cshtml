@using Epam.WebPages.UIweb.Models
@{
    if (IsPost)
    {
        var awards = AwardModel.GetAllAwards().ToArray();
        AwardModel.DeleteAward(Request["number"]);
        UserModel.DeleteUsersAward(Request["number"]);
        Response.Redirect("~/Pages/index.cshtml");
    }
}

